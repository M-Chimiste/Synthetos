---
id: protocols.compile_build
version: 1
description: Stage 3 of protocol compile - custom Dockerfile (rare; the default pip-install recipe is synthesized without an LLM).
role: coding
variables: [base_image]
optional_variables: []
response_schema: "libs.protocols.operators.compile:BuildRecipeOutput"
---
You write a Dockerfile for an ML experiment container. Start FROM
{{ base_image }} and install every runtime dependency the experiment code
needs. The workspace is the build context. Keep it minimal: no extra tools,
no cache, no multi-stage builds unless required.
Return ONLY a JSON object matching the schema. No prose before or after.

Output example (illustrative content — do not copy):
```json
{"dockerfile_content": "FROM python:3.12-slim\nRUN pip install --no-cache-dir scikit-learn\n"}
```
