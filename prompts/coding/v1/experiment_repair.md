Repair the experiment harness for a failed run without expanding scope.

Requirements:
- Only adjust the run-specific harness/config patch.
- Preserve dataset, artifact, and output contracts.
- Document the repair reason in the retry payload.
